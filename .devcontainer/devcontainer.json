{
  "name": "DuckDB + Rye",
  "dockerFile": "Dockerfile",
  "postCreateCommand": "rye sync", // Installing Python dependencies need project files
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "charliermarsh.ruff"],
      "settings": {
        "python.defaultInterpreterPath": ".venv/bin/python"
      }
    }
  }
}
